function setup() {
  createCanvas(640, 480);
//background(red, green, blue);

  }
// draw function
function draw(){
  background(255, 0, 0);
if (mouseX < 300) {
  
  stroke(255);
  noFill();

  ellipse(200, 200, 100, 100);
}
 
}